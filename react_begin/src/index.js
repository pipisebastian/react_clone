import React from 'react';
import ReactDOM from 'react-dom';
import App from './6_4';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
//다음과 같은 컴포넌트를 root안에 넣어줌!!
